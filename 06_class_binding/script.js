Vue.createApp({
  data() {
    return {
      activeLetter: "A",
    };
  },
}).mount("#app");

/*methods: {
    renderCurrBtn(activeLetter) {
      if (activeLetter === "A") {
        return "btn-active";
      } else if (activeLetter === "B") {
        return "btn-active";
      } else if (activeLetter === "C") {
        return "btn-active";
      }
    },
  },*/
